"use strict";
var __extends = (this && this.__extends) || (function () {
    var extendStatics = function (d, b) {
        extendStatics = Object.setPrototypeOf ||
            ({ __proto__: [] } instanceof Array && function (d, b) { d.__proto__ = b; }) ||
            function (d, b) { for (var p in b) if (b.hasOwnProperty(p)) d[p] = b[p]; };
        return extendStatics(d, b);
    };
    return function (d, b) {
        extendStatics(d, b);
        function __() { this.constructor = d; }
        d.prototype = b === null ? Object.create(b) : (__.prototype = b.prototype, new __());
    };
})();
var __assign = (this && this.__assign) || function () {
    __assign = Object.assign || function(t) {
        for (var s, i = 1, n = arguments.length; i < n; i++) {
            s = arguments[i];
            for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p))
                t[p] = s[p];
        }
        return t;
    };
    return __assign.apply(this, arguments);
};
var __rest = (this && this.__rest) || function (s, e) {
    var t = {};
    for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p) && e.indexOf(p) < 0)
        t[p] = s[p];
    if (s != null && typeof Object.getOwnPropertySymbols === "function")
        for (var i = 0, p = Object.getOwnPropertySymbols(s); i < p.length; i++) {
            if (e.indexOf(p[i]) < 0 && Object.prototype.propertyIsEnumerable.call(s, p[i]))
                t[p[i]] = s[p[i]];
        }
    return t;
};
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
var react_1 = require("react");
var prop_types_1 = __importDefault(require("prop-types"));
var leaflet_1 = __importDefault(require("leaflet"));
var PropTypes_1 = require("../../util/PropTypes");
var Context_1 = __importDefault(require("../RCMap/Context"));
var TileLayer = /** @class */ (function (_super) {
    __extends(TileLayer, _super);
    function TileLayer(props, context) {
        var _this = _super.call(this, props, context) || this;
        var url = props.url, options = __rest(props, ["url"]);
        _this.instance = leaflet_1.default.tileLayer(url, options);
        context.map.addLayer(_this.instance);
        return _this;
    }
    TileLayer.prototype.shouldComponentUpdate = function (nextProps) {
        var _a = this.props, url = _a.url, zIndex = _a.zIndex, opacity = _a.opacity;
        var nextUrl = nextProps.url, nextZIndex = nextProps.zIndex, nextOpacity = nextProps.opacity;
        return !(url === nextUrl && zIndex === nextZIndex && opacity === nextOpacity);
    };
    TileLayer.prototype.componentDidUpdate = function (prevProps) {
        var prevUrl = prevProps.url, prevZIndex = prevProps.zIndex, prevOpacity = prevProps.opacity;
        var _a = this.props, url = _a.url, zIndex = _a.zIndex, opacity = _a.opacity;
        var tileLayer = this.instance;
        if (url !== prevUrl) {
            tileLayer.setUrl(url);
        }
        if (zIndex !== prevZIndex) {
            tileLayer.setZIndex(zIndex);
        }
        if (opacity !== prevOpacity) {
            tileLayer.setOpacity(opacity);
        }
    };
    TileLayer.prototype.componentWillUnmount = function () {
        this.instance.remove();
    };
    TileLayer.prototype.render = function () {
        return null;
    };
    TileLayer.propTypes = __assign({}, PropTypes_1.TileLayerOptionsShape, { url: prop_types_1.default.string.isRequired });
    TileLayer.contextType = Context_1.default;
    return TileLayer;
}(react_1.Component));
exports.default = TileLayer;
