var __extends = (this && this.__extends) || (function () {
    var extendStatics = function (d, b) {
        extendStatics = Object.setPrototypeOf ||
            ({ __proto__: [] } instanceof Array && function (d, b) { d.__proto__ = b; }) ||
            function (d, b) { for (var p in b) if (b.hasOwnProperty(p)) d[p] = b[p]; };
        return extendStatics(d, b);
    };
    return function (d, b) {
        extendStatics(d, b);
        function __() { this.constructor = d; }
        d.prototype = b === null ? Object.create(b) : (__.prototype = b.prototype, new __());
    };
})();
var __assign = (this && this.__assign) || function () {
    __assign = Object.assign || function(t) {
        for (var s, i = 1, n = arguments.length; i < n; i++) {
            s = arguments[i];
            for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p))
                t[p] = s[p];
        }
        return t;
    };
    return __assign.apply(this, arguments);
};
import { Children, isValidElement, cloneElement } from 'react';
import PropTypes from 'prop-types';
import L from 'leaflet';
import Layer from '../Layer';
var LayerGroup = /** @class */ (function (_super) {
    __extends(LayerGroup, _super);
    function LayerGroup() {
        return _super !== null && _super.apply(this, arguments) || this;
    }
    LayerGroup.prototype.createInstance = function (props) {
        return L.layerGroup();
    };
    LayerGroup.prototype.render = function () {
        var _a = this.props, zIndex = _a.zIndex, children = _a.children;
        var group = this.instance;
        return children ? Children.map(children, function (child) { return (isValidElement(child) ? cloneElement(child, __assign({ zIndex: zIndex, zIndexOffset: zIndex }, child.props, { group: group, layer: group })) : child); }) : null;
    };
    LayerGroup.propTypes = __assign({}, Layer.propTypes, { zIndex: PropTypes.number });
    return LayerGroup;
}(Layer));
export default LayerGroup;
