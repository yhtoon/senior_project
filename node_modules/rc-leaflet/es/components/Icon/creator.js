var __assign = (this && this.__assign) || function () {
    __assign = Object.assign || function(t) {
        for (var s, i = 1, n = arguments.length; i < n; i++) {
            s = arguments[i];
            for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p))
                t[p] = s[p];
        }
        return t;
    };
    return __assign.apply(this, arguments);
};
import L from 'leaflet';
import baseOptions from '../BaseIcon/options';
import iconUrl from '../../assets/images/point-default.png';
import shadowUrl from '../../assets/images/point-shadow.png';
var defaultOptions = __assign({ iconUrl: iconUrl,
    shadowUrl: shadowUrl }, baseOptions, { shadowSize: [41, 41], shadowAnchor: [12, 41] });
var creator = function (options) {
    if (!options.iconUrl || options.iconUrl === iconUrl) {
        return L.icon(__assign({}, defaultOptions, options));
    }
    return L.icon(options);
};
var defaultIcon = creator(defaultOptions);
export default creator;
export { defaultOptions, defaultIcon };
