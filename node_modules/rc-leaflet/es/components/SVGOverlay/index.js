var __extends = (this && this.__extends) || (function () {
    var extendStatics = function (d, b) {
        extendStatics = Object.setPrototypeOf ||
            ({ __proto__: [] } instanceof Array && function (d, b) { d.__proto__ = b; }) ||
            function (d, b) { for (var p in b) if (b.hasOwnProperty(p)) d[p] = b[p]; };
        return extendStatics(d, b);
    };
    return function (d, b) {
        extendStatics(d, b);
        function __() { this.constructor = d; }
        d.prototype = b === null ? Object.create(b) : (__.prototype = b.prototype, new __());
    };
})();
var __rest = (this && this.__rest) || function (s, e) {
    var t = {};
    for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p) && e.indexOf(p) < 0)
        t[p] = s[p];
    if (s != null && typeof Object.getOwnPropertySymbols === "function")
        for (var i = 0, p = Object.getOwnPropertySymbols(s); i < p.length; i++) {
            if (e.indexOf(p[i]) < 0 && Object.prototype.propertyIsEnumerable.call(s, p[i]))
                t[p[i]] = s[p[i]];
        }
    return t;
};
import L from 'leaflet';
import ImageOverlayClass from '../ImageOverlay/Class';
import Content from './Content';
var uri = 'http://www.w3.org/2000/svg';
var SVGOverlay = /** @class */ (function (_super) {
    __extends(SVGOverlay, _super);
    function SVGOverlay() {
        return _super !== null && _super.apply(this, arguments) || this;
    }
    SVGOverlay.prototype.createInstance = function (props) {
        var bounds = props.bounds, options = __rest(props, ["bounds"]);
        var svg = document.createElementNS(uri, 'svg');
        svg.setAttribute('xmlns', uri);
        return L.svgOverlay(svg, bounds, options);
    };
    SVGOverlay.Content = Content;
    return SVGOverlay;
}(ImageOverlayClass));
export default SVGOverlay;
